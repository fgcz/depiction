*.egg-info
*.pyc
*.png
.DS_Store
.hypothesis
.idea
.ipynb_checkpoints
.snakemake
.task
.venv
.vscode
.python-version
.nox
dist
_build
# build/ in any package, not just the root (workspace members build too)
build/
__pycache__

*.ibd
*.imzML
# imzy's offset cache, written next to whatever imzML it just read
*.icache
/build/

# release artifact, regenerated by `uv export` at deploy time
pylock.toml

# downloaded public test acquisitions -- see tests/real_data/fetch.py. The imzML/ibd pair
# is already covered above; this also catches .part leftovers and imzy's .icache sidecars.
/.test-data/
